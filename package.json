{
  "name": "bitcoin-desktop",
  "version": "0.0.1",
  "description": "Bitcoin desktop client — full HTFU verification, headers to full node",
  "type": "module",
  "main": "main.js",
  "scripts": {
    "start": "electron . --no-sandbox --disable-gpu",
    "test": "node --test test/*.test.js",
    "dev": "electron . --dev",
    "web": "npx serve ."
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bitcoincc/bitcoin-desktop"
  },
  "license": "AGPL-3.0-or-later",
  "devDependencies": {
    "electron": "^41.0.2"
  },
  "dependencies": {
    "hash-wasm": "^4.12.0"
  }
}
